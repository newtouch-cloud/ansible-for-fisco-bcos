[group]
group_id={{ agency.group_id }}

[nodes]
{% set ns = namespace(seq=0) %}
{% for a in agencies if a.group_id == agency.group_id %}
{% for node in a.nodes %}
node{{ ns.seq }}={{ node }}:30300
{% set ns.seq = ns.seq + 1 %}
{% endfor %}
{% endfor %}
